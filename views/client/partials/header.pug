nav(class="navbar navbar-expand-lg navbar-light bg-white px-lg-3 py-lg-2 shadow-sm sticky-top")
  div(class="container-fluid")
    a(class="navbar-brand me-5 fw-bold fs-3 h-font" href="/") Music
    
    div(class="collapse navbar-collapse" id="navbarSupportedContent")
      ul(class="navbar-nav me-auto mb-2 mb-lg-0")
        li(class="nav-item")
          a(
            class="nav-link me-2"
            href="/" 
            aria-current="page"
          )| Trang chủ
        li(class="nav-item")
          a(class="nav-link me-2"  href="/topics") Chủ đề
        li(class="nav-item")
          a(class="nav-link me-2" href="/songs") Bài hát 
        li(class="nav-item")
          a(class="nav-link me-2" href="/favorite-songs") Yêu thích
        li(class="nav-item")
          a(class="nav-link me-2" href="/about") Giới thiệu
      
      .box-search.mx-5
        form(
          action="/search/result"
          method="GET"
        )
          div(class="form-group d-flex mb-0")
            input(
              type="text"
              placeholder="Nhập từ khóa..."
              class="form-control"
              name="keyword"
              value=keyword
            )
            button(
              type="submit"
              class="btn btn-primary"
            ) Tìm
          .inner-suggest
            .inner-list

            
      div(class="d-flex")
        if(user)
          .btn-group
            button.btn.btn-outline-dark.dropdown-toggle(
              aria-expanded="false" 
              data-bs-display="static" 
              data-bs-toggle="dropdown" 
              type="button"
              )
              span #{user.fullName}
            ul.dropdown-menu.dropdown-menu-lg-end
              li
                a.dropdown-item(href="/user/profile") Hồ sơ 
              li
                a.dropdown-item(href="/user/bookings") Phòng đã đặt
              li
                a.dropdown-item(href="/user/logout") Đăng xuất
        else 
          a(
            class="btn btn-outline-dark shadow-none me-lg-3 me-2"
            href="/user/login"
          ) Đăng nhập
          a(
            class="btn btn-outline-dark shadow-none"
            href="/user/register"
          ) Đăng kí